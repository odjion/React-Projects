import React from 'react';
import {View, Text, Image, ScrollView, TextInput} from 'react-native';

const YourApp = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>
      "Na filosofia, aprendemos que a jornada é mais importante que o destino.
      Da mesma forma, a programação é uma busca constante:
      cada teste, cada erro, é uma parte do processo evolutivo, 
      assim como o gato que segue seu instinto sem pressa. 🐾💡"

 </Text>

       <Image
          source={{
            uri: 'https://reactnative.dev/docs/assets/p_cat2.png',
          }}
          style={{width: 200, height: 200}}
        />

    </View>
  );
};

export default YourApp;
